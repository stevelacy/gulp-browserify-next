'use strict';

var should = require('should');
var browserify= require('../');

require('mocha');

describe('gulp-browserify-next', function() {
  describe('FUNCTIONNAME()', function() {
    it('should TASKNAME', function(done) {
      should.exist(browserify);
      done();
    });
  });
});
